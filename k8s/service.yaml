apiVersion: v1
kind: Service
metadata:
  name: giirs-service
  labels:
    app: giirs-app
spec:
  selector:
    app: giirs-app
  type: NodePort
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8000
    nodePort: 30080
