apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: giirs-app-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: giirs-app
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
